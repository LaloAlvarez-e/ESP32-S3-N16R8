# common_utils

Shared utility library root.

- Build this folder as a static library.
- Treat each child folder as a module boundary.
- Use [common_utils.h](common_utils.h) as the folder-level aggregator header.
- Keep unit tests isolated in [test/README.md](test/README.md).
