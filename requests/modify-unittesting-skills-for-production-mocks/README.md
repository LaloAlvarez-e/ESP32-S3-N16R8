# Request README

## Request

Modify the unit-testing skills so tests use production code, mock called implementations with GoogleTest and GoogleMock, use test-specific visibility macros when needed, and live inside isolated `test/` folders within each owning project or common library.

## Result

Completed. The skill, references, template assets, and request tracker were updated to reflect the required unit-testing model.
