# Plan

1. Convert the attach-only launch profiles into real late-attach profiles.
2. Add matching prepare tasks that do not depend on early-startup breakpoint timing.
3. Validate the new attach-and-halt sequence against a live OpenOCD server.
