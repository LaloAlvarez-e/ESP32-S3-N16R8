# Request

Add a short debug cheat sheet to the repo with exact GDB commands for common ESP32-S3 register and memory inspection, and recheck the ESP-IDF panels that can be used from this workspace.

## Current Result

Completed.

- The repository now includes `docs/esp32-s3-gdb-cheat-sheet.md`.
- The root and hello world README files now describe the ESP-IDF views relevant to this workspace and how to reopen them.
- The documentation now clarifies that view visibility is stored by VS Code as local workspace UI state rather than a shareable repo setting.
