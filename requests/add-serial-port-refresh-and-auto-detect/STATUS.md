# Status

## State

Completed

## Summary

The repository now provides a serial-port listing script and VS Code task, and the ESP-IDF wrapper can safely auto-select the serial port when exactly one COM port is detected. The workspace serial-port setting now uses `AUTO` instead of a stale fixed COM value.
