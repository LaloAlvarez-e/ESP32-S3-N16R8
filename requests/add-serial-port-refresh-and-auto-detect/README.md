# Request README

## Request

Explain how to recheck available serial ports when the board port changes and make the workspace easier to refresh automatically.

## Current Result

Completed. The workspace can now list current COM ports on demand, and flash or monitor tasks can use automatic serial-port selection when exactly one board port is present.
