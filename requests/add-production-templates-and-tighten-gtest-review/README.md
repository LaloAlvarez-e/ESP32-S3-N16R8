# Request README

## Request

Add a production header and source template that already includes the `UNIT_TEST`, `STATIC`, and `WEAK` pattern, and update the GoogleTest Review agent so it enforces the repository mocking and isolation rules explicitly.

## Result

Completed. The new template assets were added under the embedded coding skill, and the GoogleTest Review agent now checks the newer testing rules explicitly.
