# Plan

## Objective

Add production header and source templates that already include the `UNIT_TEST`, `STATIC`, and `WEAK` pattern, and update the GoogleTest Review agent so it enforces the repository mocking and isolation rules explicitly.

## Steps

1. Add the production header and source template assets to the embedded coding skill.
2. Update the embedded coding skill to reference the new templates.
3. Update the GoogleTest Review agent to enforce the current production-code, mock, and isolation rules.
4. Record the completed work in the request tracker.

## Completion

All planned steps were completed in this request.
