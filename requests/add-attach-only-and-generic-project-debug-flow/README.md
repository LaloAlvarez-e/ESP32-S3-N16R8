# Request

Add attach-only debug profiles, improve the current visual debugging flow, and make new ESP32 projects easier to debug without having to keep editing launch JSON.

## Outcome

The workspace now offers safer attach-only profiles for existing projects and generic prompt-driven tasks and launch configurations for new ESP-IDF projects. Editing `launch.json` is no longer necessary for normal new-project bring-up when the same board and toolchain settings apply.