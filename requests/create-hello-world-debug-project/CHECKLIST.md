# Checklist

- [x] Create the request folder and required tracking files.
- [x] Add the standalone `hello_world_project/` ESP-IDF project.
- [x] Convert `sample_project/` into a standalone ESP-IDF project entry.
- [x] Convert `common_utils/`, `common_os/`, and `common_driver/` into ESP-IDF-compatible shared components.
- [x] Add VS Code settings, tasks, launch configurations, and helper scripts for build, flash, OpenOCD, and ELF load.
- [x] Validate the resulting setup and close the tracker.
