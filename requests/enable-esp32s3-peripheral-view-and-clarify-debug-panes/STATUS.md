# Status

Completed.

- Confirmed that the ESP-IDF extension contributes custom Peripheral and Hex views for `gdbtarget` sessions.
- Added `.vscode/svd/esp32s3.svd` from Espressif's SVD repository and pointed `idf.svdFilePath` at it.
- Updated workspace documentation to explain that the ESP-IDF Peripheral and Hex views are the supported inspection panes for the current `gdbtarget` flow.
- Validated the edited settings and documentation files with no reported errors.
