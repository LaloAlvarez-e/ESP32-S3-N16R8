# Plan

1. Verified which debug panes are provided by the current ESP-IDF `gdbtarget` session and which ones are adapter-specific.
2. Added the missing ESP32-S3 SVD file and wired `idf.svdFilePath` so the ESP-IDF Peripheral View can populate.
3. Updated workspace documentation so the correct panes and workflows are clear.
4. Validated the workspace changes and recorded the result.
