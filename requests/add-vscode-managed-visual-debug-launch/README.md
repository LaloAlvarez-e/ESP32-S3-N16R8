# Request

Explain whether the ESP32 debug flow can feel more like an IDE inside VS Code, and add a more visual launch option if possible.

## Outcome

It is possible. New VS Code-managed launch configurations were added so the debugger can start OpenOCD itself instead of requiring the user to start it manually first.