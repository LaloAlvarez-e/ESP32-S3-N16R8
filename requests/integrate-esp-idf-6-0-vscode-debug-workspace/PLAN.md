# Plan

## Objective

Complete the root-workspace ESP-IDF 6.0 integration so this VS Code workspace can build, flash, and debug ESP32-S3 projects with stable launch profiles, usable code intelligence, and documented debug-memory views.

## Steps

1. Create the mandatory request-tracking files.
2. Enable deterministic compile database generation for the standalone ESP-IDF projects.
3. Add root-workspace VS Code IntelliSense configuration for ESP-IDF headers and shared components.
4. Normalize the launch, task, and settings files around the validated ESP32-S3 USB-JTAG workflow.
5. Update the repository documentation with the final workflow and supported debug views.
6. Validate the resulting builds and workspace configuration, then record the outcome.

## Current State

Completed.