# common_driver test

Isolated unit-test target for `common_driver`.

- Compile the real production sources from `common_driver/`.
- Mock only linked hardware collaborators when isolation is needed.
- Keep test helpers local to this folder.
